import styled from "styled-components";

const MainStyledContainer = styled.div`
  width: 100%;
  height: 100%;
  padding: 70px 15px;
  background-image: linear-gradient(240deg, var(--color-secondary) 0%, var(--color-primary) 100%) ;
  display: flex;
  justify-content: center;
`;

export default MainStyledContainer;
